version: "3"

services:
  jekyll:
    image: jekyll/jekyll:4
    container_name: hal-site
    command: bash -c "bundle install && jekyll serve --livereload --host 0.0.0.0"
    ports:
      - "4000:4000"
    volumes:
      - ./docs:/srv/jekyll
    restart: unless-stopped
